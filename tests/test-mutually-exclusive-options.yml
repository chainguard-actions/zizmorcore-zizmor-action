name: Test mutually exclusive options fail
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - id: action
        uses: .
        with:
          inputs: "."
          # Both advanced-security and annotations set to true — this is invalid
          advanced-security: "true"
          annotations: "true"
          online-audits: "false"
        continue-on-error: true
      - name: Assert action failed due to mutually exclusive options
        env:
          OUTCOME: ${{ steps.action.outcome }}
        run: |
          echo "outcome: $OUTCOME"
          if [ "$OUTCOME" = "failure" ]; then
            echo "PASS: action correctly failed when both advanced-security and annotations are true"
          else
            echo "FAIL: expected failure for mutually exclusive options, got '$OUTCOME'"
            exit 1
          fi
